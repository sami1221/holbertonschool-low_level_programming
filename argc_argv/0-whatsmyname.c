#include <stdio.h>
/**
 * main - prints its name
 * Return: 0
 * @argc: number of arguments passed
 * @argv: pointer array which points to arguments
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	(void)argc;
	return (0);
}
